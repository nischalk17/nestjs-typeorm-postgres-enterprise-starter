export enum UserRoleENUM {
  SUPER_ADMIN = 'super_admin',
  ADMIN = 'admin',
  USER = 'user',
}

export enum MediaTypeEnum {
  VIDEO = 'video',
  IMAGE = 'image',
  DOCUMENT = 'document',
  PDF = 'pdf',
}

export enum SortOrder {
  ASC = 'ASC',
  DESC = 'DESC',
}
